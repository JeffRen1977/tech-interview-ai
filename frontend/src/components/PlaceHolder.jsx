import React from 'react';

const Placeholder = ({ title }) => (
    <div>
        <h1 className="text-4xl font-bold">{title}</h1>
        <p className="mt-4 text-gray-500">此功能正在开发中...</p>
    </div>
);
export default Placeholder;